# Domain Docs

This is a single-context repository.

Before changing the project:

1. Read the root `CONTEXT.md` when present.
2. Read relevant decisions under `docs/adr/`.
3. Use terminology defined in the domain glossary.
4. Surface conflicts with existing ADRs explicitly.

Missing domain files are not errors; proceed without prompting for their creation.
